# use hub with git command 
eval "$(hub alias -s)"
# release memory
alias release='sudo launchctl unload /System/Library/LaunchDaemons/com.apple.dynamic_pager.plist;sudo launchctl load /System/Library/LaunchDaemons/com.apple.dynamic_pager.plist'
